# ShellDroid release signing — template (safe to commit, no secrets)
# Copy to .env and fill in the real passwords:
#   cp .env.example .env
SHELLDROID_KEYSTORE=shelldroid-upload.jks
SHELLDROID_KEYSTORE_PASS=
SHELLDROID_KEY_PASS=
